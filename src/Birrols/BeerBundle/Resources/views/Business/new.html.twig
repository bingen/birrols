{% extends '::base.html.twig' %}

{% block body %}

<h3>Business creation</h3>

<form action="{{ path('business_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('business') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}
