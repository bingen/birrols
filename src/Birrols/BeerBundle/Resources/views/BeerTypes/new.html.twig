{% extends '::base.html.twig' %}

{% block body %}

<h3>BeerTypes creation</h3>

<form action="{{ path('beertypes_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('beertypes') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}
